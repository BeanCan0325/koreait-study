1. 브랜치란
  - 하나의 프로젝트에서 독립적인 작업을 하는 공간

2. 커맨드
 - 브랜치 생성: git branch 브랜치명
 - 브랜치 목록: git branch
 - 브랜치 이동: git switch 브랜치명
 - 커밋 합치기: git merge 합칠 브랜치명
  >마지막 커밋 시점을 가져와 적용

3.충돌상황
 - 서로 다른 개발자가 똑같은 파일에 다른 코드를 작성할 때 발생
 - git merge --abort를 작성하여 merge를 취소하거나 필요 코드만 남기고 커밋해서 해결

. ->내 현재위치 싹다
git add 파일명 해도 됨

<<<<<<< HEAD
git push origin main

다른 개발자가 코드를 추가했습니다.

3. 충돌상황(conflict)
    - git merge --abort 를 작성해서 merge



@ 하지말아야 할것
 git push -f origin main
 -> f를 넣으면 올라감 ( f: 내 코드 강제로 올리겠다.)


- dev branch만듦

-PR이라는 공간. Pull Request
